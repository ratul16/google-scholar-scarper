{
  "name": "scholar-stats",
  "type": "module",
  "version": "1.0.0",
  "description": "Daily Google Scholar citation scraper hosted on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
